Pillow>=9.0
numpy>=1.24
# GUI deps (gui.py):
# tkinter is bundled with the official Python installer (no pip install needed)
#   Linux:   sudo apt install python3-tk
#   Windows / macOS: use the python.org installer (built-in)
# Optional: enable drag & drop in the GUI
# tkinterdnd2
